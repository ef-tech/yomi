/**
 * Type declarations for public/mermaid-config.js (browser ES module).
 *
 * これにより tests/mermaid-config.test.ts から型安全に import できる。
 * 実装は public/mermaid-config.js 側にある (yomi はビルドステップなしで
 * ブラウザに直接配る哲学なので、ソースは .js のまま)。
 */

export declare const MERMAID_SECURE_KEYS: string[];
